{
  "repo": "fixtures/login-app",
  "reruns": 2,
  "cases": [
    {
      "spec": "tests/valid-login.spec.ts",
      "expected": "trusted",
      "note": "AC1 — meaningful behavioral test; mutation goes red"
    },
    {
      "spec": "tests/wrong-password.spec.ts",
      "expected": "trusted",
      "note": "AC2 — meaningful; mutation (API accepts) goes red"
    },
    {
      "spec": "tests/smoke.spec.ts",
      "expected": "rejected",
      "note": "Hollow smoke; mutation still passes → not meaningful"
    }
  ]
}
